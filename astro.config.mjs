// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  base: '/portfolio-hub/',
  site: 'https://ClementCI.github.io/portfolio-hub/'
});
